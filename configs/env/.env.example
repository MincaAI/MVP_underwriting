# Minca AI - Environment Template
# Copy this file to the appropriate environment file and fill in your values

# Environment (development, staging, production)
NODE_ENV=development
ENVIRONMENT=development
LOG_LEVEL=DEBUG

# Database (PostgreSQL + pgvector)
POSTGRES_DB=minca
POSTGRES_USER=minca
POSTGRES_PASSWORD=your_db_password_here
DATABASE_URL=postgresql+psycopg://minca:your_db_password_here@db:5432/minca

# Storage (MinIO for dev, S3 for staging/prod)
MINIO_ROOT_USER=minio
MINIO_ROOT_PASSWORD=minio12345
S3_ENDPOINT_URL=http://minio:9000
S3_BUCKET_RAW=raw
S3_BUCKET_EXPORTS=exports
AWS_ACCESS_KEY_ID=your_aws_access_key_here
AWS_SECRET_ACCESS_KEY=your_aws_secret_key_here
AWS_REGION=us-east-1

# Queue backend (local, redis)
QUEUE_BACKEND=local
REDIS_URL=redis://localhost:6379/0

# Security (for production)
CORS_ORIGINS=http://localhost:3000
ALLOWED_HOSTS=localhost,127.0.0.1
